{
  "name": "oracle-devops-platform-ui-tests",
  "version": "1.0.0",
  "description": "Dev Hub UI and Browser E2E Test Suite (Zero-Trust & Zero-Dependency)",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1"
  }
}
