# STSENAARIUM 11: TÄISISOLEERITUD PUBLISHER ENTERPRISE VIRN
# Kasutus: Publisher RCU DB + ALISE DB + Proxy DB + ORDS + Analytics Publisher.

DB_PUBLISHER=db-publisher-oracle
DB_PROXY=db-proxy-oracle
DB_ALISE=db-alise-oracle
SKIP_ORDS=false
SKIP_PUBLISHER=false
