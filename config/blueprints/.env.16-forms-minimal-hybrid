# ============================================================================
# BLUEPRINT 16: Minimaalne Hübriid (Forms + Forms RCU / APEX Proxy DB + Custom DB + ORDS)
# Kasutus: Oracle Forms 14c + Kombineeritud Forms/APEX Proxy DB (db-proxy) + Custom DB (db-lis) + ORDS
# ============================================================================

DB_LIS=db-lis-oracle
DB_PROXY=db-proxy-oracle
DB_FORMS=db-proxy-oracle
SKIP_ORDS=false
SKIP_PUBLISHER=true
SKIP_FORMS=false
ORDS_MODE=standalone
