# ============================================================================
# BLUEPRINT 15: Täielik Enterprise Forms Arhitektuur (Forms + Forms DB + Custom DB + APEX Proxy DB + ORDS)
# Kasutus: Oracle Forms 14c + Pühendatud RCU DB (db-forms) + Custom DB (db-lis) + APEX/ORDS Proxy DB (db-proxy) + ORDS
# ============================================================================

DB_LIS=db-lis-oracle
DB_PROXY=db-proxy-oracle
DB_FORMS=db-forms-oracle
SKIP_ORDS=false
SKIP_PUBLISHER=true
SKIP_FORMS=false
ORDS_MODE=standalone
