# Blueprint 22: 1-DB Compact Reporting Stack (ALISE + Publisher + ORDS + Web-IDE)
# Resource-efficient reporting stack where Analytics Publisher shares RCU schemas inside the ALISE database, fitting within 8 GB RAM.
DB_ALISE=db-alise-oracle
DB_PROXY=NONE
DB_PUBLISHER=NONE
DB_FORMS=NONE
PUBLISHER_USE_ALISE_DB=true
WEB_IDE_PROFILE=web-ide-dev
SKIP_ORDS=false
SKIP_PUBLISHER=false
SKIP_FORMS=true
